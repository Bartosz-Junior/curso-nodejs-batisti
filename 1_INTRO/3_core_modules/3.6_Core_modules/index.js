/* Sobre core modules

    Vamos ver diversos core modules em detalhes, 
    que são fundamentais para criação de softwares
    em Node;
    http: Módulo para criar servidores HTTP;
    path: Extrair informações de paths(caminho) de arquivos;
    fs: File system, leitura e escrita de arquivos;
    URL: Módulo para trabalhar com URLs*/

const fs = require('fs')

fs.mkdir('.\Meuaquivo.txt')